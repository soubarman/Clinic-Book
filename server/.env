PORT=5000
MONGO_URI=mongodb+srv://soubarman62_db_user:sourav8474@doctorbooking.loufwmq.mongodb.net/clinic_booking?appName=DoctorBooking
JWT_SECRET=clinic_booking_super_secret_key_2024
JWT_EXPIRES_IN=30d

# --- AUTH CONFIG ---
# Admin login details (admin123 is the default fallback)
ADMIN_PASSWORD=admin123

# Firebase Admin SDK (CRITICAL for Render)
# Generate this in Firebase Console > Project Settings > Service Accounts > Generate new private key
# Paste the ENTIRE JSON content here as a single line string
FIREBASE_SERVICE_ACCOUNT='{"type": "service_account", "project_id": "...", ...}'

# --- EXTERNAL SERVICES ---
# Razorpay (replace with your test keys)
RAZORPAY_KEY_ID=rzp_test_mock_key
RAZORPAY_KEY_SECRET=mock_secret

# Platform fee in paise (₹20 = 2000 paise)
PLATFORM_FEE=2000

# CORS Support (Set this to your Vercel URL in production)
# FRONTEND_URL=https://clinic-book.vercel.app
